#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define ShowPoint cout << setprecision(20) << setiosflags(ios::fixed) << setiosflags(ios::showpoint);
void FastIO()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

void ReadWriteIO()
{
    freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);
}

int inputMultiple()
{
    int t;
    cin >> t;
    return t;
}

ll x, y;

bool isValid(ll minCost){
    return 
}
void solve()
{
    cin >> x >> y;
    ll l = 0, r = 1e9;
    while(l + 1 < r){
        ll m = (l + r) / 2;

    }
}

int main()
{
    // ReadWriteIO();
    FastIO();
    // int t;
    // cin >> t;
    // while (t--)
    solve();
}